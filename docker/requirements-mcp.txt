# MDZen MCP Server Dependencies
# Note: Heavy scientific packages (openmm, rdkit, ambertools, etc.) are installed via conda

# MCP Server Framework (FastMCP 2.x for Streamable HTTP transport)
fastmcp>=2.0.0
mcp[cli]>=1.18.0

# Structure handling
pdb2pqr>=3.1.0
propka>=3.5.0
gemmi>=0.6.0
parmed>=4.3.0
dimorphite-dl>=1.3.2

# External services
pubchempy>=1.0.4
tavily-python>=0.3.0
httpx>=0.28.1
requests>=2.31.0

# Data models & validation
pydantic>=2.12.3
pyyaml>=6.0

# Utilities (for logging/config)
python-dotenv>=1.0.0
